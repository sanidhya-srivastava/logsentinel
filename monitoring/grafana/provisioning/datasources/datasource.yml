# LogSentinel — Grafana datasource provisioning
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true

  - name: Elasticsearch
    type: elasticsearch
    access: proxy
    url: http://elasticsearch:9200
    database: "logsentinel-logs-*"
    jsonData:
      esVersion: "8.0.0"
      timeField: "@timestamp"
      logMessageField: "message"
      logLevelField: "level"
    editable: true

  - name: PostgreSQL
    type: postgres
    url: postgres:5432
    user: logsentinel
    secureJsonData:
      password: logsentinel_dev
    jsonData:
      database: logsentinel
      sslmode: disable
      maxOpenConns: 5
      maxIdleConns: 2
    editable: true
