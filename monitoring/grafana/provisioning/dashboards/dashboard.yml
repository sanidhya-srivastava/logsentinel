# LogSentinel — Grafana dashboard provisioning
apiVersion: 1
providers:
  - name: logsentinel-dashboards
    orgId: 1
    folder: LogSentinel
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
